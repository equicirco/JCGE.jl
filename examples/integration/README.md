# Integration tests

These scripts check cross-package compatibility at a minimal level:
- specs can be constructed and vali
validated,
- library models can be loaded,
- expected entrypoints exist.

They are intentionally lightweight; deeper numerical tests come later.
